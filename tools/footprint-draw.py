#!/usr/bin/env python3
"""footprint-draw - Copy or draw a footprint into lib/.

Not written. It is one of the ten of T4.1 and it is a placeholder: the
processes it serves come after process 2, and nothing beyond process 2 is
built.

Running it stops, so a caller that has reached for it finds out here rather
than by producing something that looks like a result.
"""

import sys

if __name__ == "__main__":
    sys.exit("footprint-draw: not written. It serves a process that is not built")
