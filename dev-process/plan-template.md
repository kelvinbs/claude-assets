# <Name> — plan

_Created: YYYY-MM-DD._

**g0.** <plan goal>

**a0.** <plan approach>

## Required reading

**Every word of every item below is read in full immediately prior to phase
execution. Mandatory. No item is skipped, skimmed or assumed known.**

- `README.md` at the repository root — the project and its record. **Its
  Required reading section is part of this list. Every item it names is read
  in full, same rule. Failing to read them is a failure of this section.**
- `docs/process/dev-process.md` — the state machine this plan runs under:
  FSM-1 step status, FSM-2 phase creation, phase-loop, and who may set
  `complete` and `failed`
- `tools/board-build/board-build-tool.md` — the tool the work is done with

---

## p1 — <Phase Name>

**g1.**

**a1.**

| # | Step | Status |
|---|---|---|
| s1.1 | Consider the goal and approach of this phase and the project, and the project history (notes). Note how this phase fits into the larger context. Consider this phase scope in relation to the scope of other phases in this plan. Then write steps to accomplish the goal via the approach in support of that larger context. | not started |

**Notes:**
